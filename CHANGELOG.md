## v1.2.4 - 2026-09-09

### 移除「开放插件」功能

本次更新**完整移除管理员向普通用户开放插件的功能链路**，插件不再提供面向普通用户的表单与授权能力。普通用户账号体系（注册/登录、QQ/Telegram 绑定、用户公告、管理后台用户管理）保留不变。

- **管理后台**：删除 `POST /api/admin/plugins/:uuid/access` 开放开关接口；插件设置弹窗移除「是否开放为普通用户」区块。
- **用户端**：删除 `GET /api/public/plugins` 与 `/api/user/plugins*` 全部接口（插件列表、用户表单、提交/更新/删除记录）；Home 页移除「开放插件」卡片；用户中心移除「开放插件」面板与用户表单弹窗，保留账号信息、绑定与公告。
- **插件运行时**：`proto3/sillygirl.js`、`sillygirl.py`、`sillygirl.d.ts` 删除 `user.Form`、`user.getUserList()`、`user.getUser()` 客户端 API 及 `SillyGirlUser` 相关类型；配置预加载运行时不再检测 `user.Form` 调用（`HasUserForm` 字段删除），`.test()` 远程验证器沙箱一并移除。
- **数据结构**：`common.Function` 删除 `Open`、`HasUserForm` 字段；插件市场列表不再返回 `open`、`has_user_form`；`__plugin_users__` 运行时桶及 `plugin_user_form_schemas`、`plugin_user_form_records`、`plugin_open` 存储桶不再被读写。
- **文档**：插件编写指南删除「user.Form 普通用户表单」与「普通用户列表」章节；API 文档删除对应接口；插件编写 skill 同步清理。

> 已有的 `plugin_open`、`plugin_user_form_schemas`、`plugin_user_form_records` 桶数据将不再被读取；如需清理可手动删除，保留也不影响运行。使用 `user.Form` 的旧插件需改用 `plugin.Form` 管理员配置表单。

## v1.2.3 - 2026-09-09

### 移除 SmallCat 面板功能

本次更新**完整移除 SmallCat（smallcat）面板集成**，项目此后仅支持青龙（qinglong）与呆呆（daidai）两类容器面板。

- **管理后台**：容器管理页面只保留青龙/呆呆两类面板的增删改查与连接检测；`GET /api/admin/panels` 只返回 `qinglong`、`daidai` 两组集合；删除 `GET /api/admin/panels/:id/accounts` 接口。
- **用户端**：删除普通用户的 SmallCat 相关接口（smallcat-panels、smallcat-login-sessions、smallcat-accounts、smallcat-verification-codes、插件 smallcat-accounts 与授权接口）及用户页面的「smallcat 账号」卡片、扫码登录弹窗；账号绑定只保留 QQ 与 Telegram。
- **插件授权**：删除 `__plugin_smallcat_authorized__` 运行时桶与 `smallcat:read` 授权范围；插件开放条件改由 `HasUserForm`（是否声明用户表单）单独决定，用户表单功能不受影响。
- **插件运行时**：`proto3/sillygirl.js`、`sillygirl.py`、`sillygirl.d.ts` 删除 `container.SmallCat` 客户端类及其全部方法；`container.getList()` 只返回青龙/呆呆；用户绑定结构移除 `smallcat_openids` 字段；插件源码不再检测 SmallCat 调用（`UsesSmallCat` 字段删除）。
- **面板状态检测**：刷新面板状态的能力从 smallcat 扩展到青龙/呆呆——新增 `refreshQinglongPanelsStatus`、`refreshDaidaiPanelsStatus` 并发检测，刷新面板列表时同步更新在线状态。
- **数据迁移**：启动迁移不再处理 smallcat 面板桶与 smallcat 授权数据；用户绑定迁移不再合并 `smallcat_openid(s)` 字段；旧插件配置中的 `smallcat_auth` 键会在迁移时被清理。
- **文档**：插件编写指南、API 文档、插件编写 skill 同步删除 SmallCat 章节，容器相关说明只保留青龙/呆呆。
- **残留清理**：重新构建前端嵌入资产并清除含 SmallCat 引用的旧构建产物（构建输出目录不自动清空）；资产冒烟脚本的懒加载视图数量改为从源码目录推导（跟随视图增减，不再硬编码）；`TestRegisterNodePluginAndUserForms` 测试夹具改为测试内自建，不再依赖被 gitignore 的 `plugins/userFormTest.js` 本地文件。

### 发布前审计修复

- **面板状态刷新**：修复刷新面板状态时成功结果被丢弃的问题——面板从离线恢复后刷新仍显示 `offline`；现在成功时回写在线状态与「最后检测」时间，失败时也更新检测时间。
- **呆呆面板刷新按钮**：修复呆呆容器页签的「刷新」不触发状态检测的问题（此前只有青龙页签会调用状态检测接口）；两个页签的刷新现在都会并发检测青龙/呆呆面板在线状态。
- **用户插件列表**：删除 `GET /api/user/plugins` 响应中恒为空的 `authorized`、`authorization_scope` 死字段，直接返回开放插件记录。
- **文档**：插件编写 skill 的质量门槛移除已删除的 SmallCat 方法 `checkQr` 引用。

> 已有 smallcat 面板数据与用户 openid 绑定将不再被读取；如后续需要清理，可在管理后台手动删除，或保留（不影响运行）。

## v1.2.2 - 2026-09-09

### 项目迁移

- 仓库迁移至 [qninq/sillyGirlPro](https://github.com/qninq/sillyGirlPro)：Go module 路径、版本检查、自更新下载源、一键安装脚本、CI 构建与 Docker Hub 镜像名同步更新。
- 修复 `go run` 或未注入构建信息的二进制版本号显示为 1.1.9 的问题：`core/version.go` 兜底常量与 `core/compile_time.go` 默认值随版本同步。
- 前端开源地址、默认仓库地址与前端版本号同步更新。

### 文档

- README 新增快速开始（源码构建 / 一键安装 / Docker / 管理后台入口），修正 Go 版本要求为 1.26+。
- 镜像地址与示例统一指向 `qninq/sillygirl`。

## v1.2.1 - 2026-09-08

### 消息管理

- 管理后台菜单「转发/回复/监听」更名为「消息管理」。
- 转发群组、回复规则、监听规则（监听群组/禁言群组/屏蔽用户）列表均新增「启用」开关，可即时停用/启用单条规则，立即生效。
- 回复规则新增 `en
able` 字段：旧数据无该字段时视为启用，不影响既有规则。

### 转发（搬运）功能增强

- 转发群组支持直接配置「转发目标」（可添加多个），消息命中来源群后由核心直接推送到目标群，无需再编写 `@carry` 处理脚本。
- 来源支持监控单聊会话：Web 会话（按 rid）、C2C 私聊等无群号渠道按会话/用户 ID 匹配，此前仅群聊可被监控转发。
- 转发目标支持 **群聊/私聊** 类型选择：群聊按群号（QQ 官方为 `group_openid`）推送，私聊按用户 openid 推送。
- 管理后台「转发」表格新增转发目标列，转发表单支持增删目标行；处理脚本配置从表单移除（API 与既有脚本配置仍兼容）。
- 修复编辑转发群组时群号无法修改的问题：保存时改存到新群号并清理旧记录，新群号重复时提示冲突。
- 目标与来源相同时自动跳过，防止消息回环；目标平台未启用或发送失败时记录错误日志，转发日志附带平台返回的消息 ID 便于排查。
- 核心推送显式携带 `chat_type` 并透传附加字段给适配器（`Factory.Push` → `SetVar`）。
- Web Bot 主动推送支持按 `chat_id` 回退投递到 Web 会话（rid），不再要求必须带 `user_id`。
- 微信 ClawBot 主动推送复用该联系人最近一次收到消息的 `context_token`（30 分钟有效），机器人收到过对方消息后即可主动转发。

### QQ 官方机器人修复

- 修复主动推送把群 `group_openid` 当频道 ID 发送报「频道不存在」（11263）的问题：核心推送显式携带 `chat_type`（群聊/私聊）场景，适配器优先按它路由到群接口 `/v2/groups/{id}/messages` 或 C2C 接口；同时从收到的群聊/C2C 消息学习「openid → 场景」映射（上限 2048 条）兜底 `pushAdmin` 等无场景信息的推送，未知目标仍走频道接口。

## v1.2.0 - 2026-09-07

### 安全修复

- 修复实时日志 SSE 端点（`/api/admin/logs/stream`）未鉴权的问题：管理员 JWT 校验现支持 `token` 查询参数回退（EventSource 无法携带自定义请求头），未携带有效令牌的请求返回 401。

### 稳定性修复

- 修复插件交互监听（`s.listen`）把返回的消息 ID 当文本回显给用户的问题：handle 返回值为 QQ 官方消息 ID 时不再回显。
- QQ 官方机器人主动推送（如 `pushAdmin` 管理员通知）无频道 ID 时自动回退 C2C 私聊接口发送，不再直接失败。

### QQ 官方机器人能力增强

- 新增扫码添加机器人：BOT 设置新增「扫码添加机器人」按钮，生成 QQ 官方绑定二维码，手机 QQ 扫码确认后自动解密并保存 AppID/AppSecret、启动机器人，并把扫码人自动加入管理员，免去手动复制凭据（密钥全程 AES-256-GCM 加密传输，服务端解密后落库；绑定服务地址可通过 `QQGUILD_BIND_HOST` 覆盖）。

- 群聊和 C2C 私聊支持富媒体回复：解析回复内容中的 `[CQ:image/video/record/file,url=...]`，通过 QQ v2 富媒体接口上传（图片/视频/语音/文件）后以 `msg_type=7` 发送，文字随首条媒体一起发送；频道场景支持单张图片 URL 直发。
- 群聊和 C2C 私聊支持 Markdown 回复：新增 `qqguild.markdown` 配置项（默认关闭，位于公域机器人开关下方），开启后回复改用 `msg_type=2` Markdown 发送，无权限时自动回退纯文本。
- 新增 `qqguild.at` 配置项（默认开启）：群聊 Markdown 回复自动 @ 发送者。
- 新增入站消息去重（10 秒窗口）：网关断线重连重推同一条消息时不再重复处理和回复。
- 支持消息撤回：接收消息时记录场景与目标上下文，通过核心 Action（`delete_message`）按场景调用对应撤回接口（群/C2C/频道/频道私信）。
- WebSocket 重连改为指数退避（2s 起步、30s 封顶，连接成功后重置），避免 QQ 网关异常时高频重连。

### 指令列表

- 管理后台新增「指令列表」页面，位于实时日志下方，汇总展示内置指令与插件指令的触发词、用途、来源和状态。
- 内置命令覆盖消息消费循环中硬编码的管理指令（群聊/私聊的 `listen`、`unlisten`/`nolisten`、`reply`、`noreply`/`unreply`），标记为只读，不可修改管理员限制。
- 页面提供内置/插件/仅管理员数量统计标签、关键词搜索（指令、触发词、插件或路径）与来源筛选（内置/插件）。
- 每条指令支持「管理员限制」开关，可覆盖脚本头部 `[admin]` 声明；开关结果持久化存储，立即生效，重启与插件热重载后依然保留。
- 新增指令列表与管理员限制切换 API（`GET /api/admin/command-list`、`POST /api/admin/command-list/:key/admin`）。

### QQ 频道机器人修复

- 修复公域机器人开关保存后不回显的问题（`qqguild.public_bot` 未加入管理端配置返回列表）。
- `GROUP_MESSAGE_CREATE` 群聊全量消息兜底解析改用 `WSMessageData` 结构，与群聊全量消息实际字段结构一致，避免解析失败导致群聊消息丢失。

### 日志优化

- BotGo SDK 心跳日志（Heartbeat）不再打印，减少实时日志与文件日志中的冗余输出。

## v1.1.9 - 2026-09-01

### 实时日志

- 管理后台新增「实时日志」页面，位于插件市场下方，支持 SSE 实时流式日志输出。
- 日志终端暗色风格，按日志级别着色（ERROR 红色、WARN 黄色、INFO 蓝色、DEBUG 灰色）。
- 支持关键词过滤、自动滚动、暂停/继续和清空日志功能。
- 后端新增 WebLogger 日志适配器，捕获所有日志输出并通过环缓冲和 SSE 订阅机制推送到前端。

### QQ 频道机器人修复

- `MessageEventHandler`（`MESSAGE_CREATE` 事件）改为按需注册，新增 `qqguild.public_bot` 配置项（默认关闭），位于沙箱开关下方。开启后注册 `IntentGuildMessages`（1 << 9）支持公域机器人接收频道全量消息；关闭时仅使用 `AT_MESSAGE_CREATE` 接收 @ 消息，避免私域机器人 `4014: disallowed intents` 连接被拒。
- 新增 `PlainEventHandler` 兜底处理器，处理 BotGo SDK v0.2.1 原生不支持的 `GROUP_MESSAGE_CREATE`（群聊全量消息）事件，数据结构和 `GROUP_AT_MESSAGE_CREATE` 一致，已接入同一消息处理链路，修复群聊全量消息无法回复的问题。

## v1.1.6 - 2026-08-11

### 普通用户插件授权管理

- 用户管理页新增普通用户已授权插件展示。
- 管理员可通过插件授权弹窗手动添加或删除用户授权。
- 新增管理员插件授权接口、前后端状态同步及回归测试。

## v1.1.5 - 2026-08-10

### 定时任务接收目标

- 定时任务新增私聊用户与群聊两种接收类型，可按平台保存用户 ID 或群号/群聊 OpenID。
- 编辑已有任务时自动识别接收类型，并为不同目标展示对应的输入提示。

## v1.1.4 - 2026-08-09

### 定时任务稳定性

- 插件注释 Cron 与后台定时任务统一增加同任务防重叠执行；上一轮尚未结束时跳过本轮调度，避免慢任务持续堆叠插件进程。
- 补充并发回归测试，覆盖原始调度并发启动和修复后仅保留单次运行的行为。

### 构建与安全

- 更新前端锁定依赖中的 `nanoid` 至 `3.3.18`，清除依赖审计报告中的高危无限循环问题。

## v1.1.2 - 2026-08-08

### 插件安装

- 插件市场改为先下载插件源码，再读取实际 `[depe]` 注释并提示安装缺失依赖。
- 依赖模块按运行时、发布者目录和文件名检查；同一发布者已安装的模块不再重复提示或下载。

### 登录鉴权

- 管理员与普通用户统一改用 `token: <JWT>` 请求头认证，前端分别持久化登录令牌并在所有受保护请求中自动携带。
- 移除认证 Cookie、`Authorization: Bearer` 与管理员原始会话令牌兼容入口，补齐角色隔离、JWT 格式、过期时间和注销校验。
- 修复服务重启后有效管理员会话未完整恢复的问题，并为会话缓存增加并发保护与持久化回源。

### 测试与构建

- 逐项验证 70 个管理员接口与 17 个用户接口对缺失、错误请求头令牌的拒绝行为，补充有效令牌、跨角色和登录响应测试。
- 重新构建嵌入式管理后台资源，并通过 Go 全量测试、vet、TypeScript 与静态资源 smoke test。

## v1.1.1 - 2026-08-08

### 定时任务

- 插件 Cron 虚拟任务支持独立保存接收平台和接收人，定时触发与手动运行使用同一收件配置。
- 关闭的定时任务点击运行时直接显示启用提示，避免未处理的接口异常进入控制台。

### 依赖管理

- 依赖列表移除插件、文件名、来源和类型列，仅保留依赖名称、版本、状态与操作。

## v1.1.0 - 2026-08-08

### 插件市场与运行状态

- 插件启停统一由源码注释 `[status: true|false]` 控制，插件卡片新增开启/关闭按钮；Cron 任务保留独立状态，不再与插件运行状态混用。
- 新增「依赖模块」分类和 `[module: true]` 模块插件；`[depe]` 中的 `./xxx.js` / `./xxx.py` 作为模块依赖，安装插件时递归提示并自动安装。
- 插件映射目录按作者分层为 `/data/plugins/<作者>/<插件>`，作者共享模块放在同一作者目录，npm/Python 公共依赖继续保留在插件根目录。
- 本地插件编辑、卸载与依赖检测完成收拢，保存源码时保留公开分类，并阻止删除仍被其他插件引用的模块。

### 机器人与稳定性

- QQ 官方机器人 WebSocket 补齐群聊与 C2C 事件及回复 API，增加配置重启防抖、Intent 校验和 BotGo 敏感日志脱敏。
- OneBot 接收端忽略心跳等非消息事件，并兼容字符串与消息段数组，避免空消息进入核心处理链。
- 插件适配器卸载按 UUID 实际销毁对应实例，避免插件重载后残留旧 Adapter。

### 清理与质量

- 删除无生产入口的插件静态文件路由、空兼容函数、旧 Proto3 辅助脚本、注释代码和未使用的前端导出。
- 收拢前端 API 数据解包、插件状态按钮、卸载弹窗和依赖安装提示，减少控制器重复逻辑。
- 补充 Go、TypeScript、Python、插件目录布局、模块依赖和 QQ 消息链路回归测试。

## v1.0.9 - 2026-08-07

### 普通用户与管理后台

- 完善普通用户插件展示、用户表单和 SmallCat 授权交互，插件设置支持独立控制是否向普通用户开放。
- 优化容器、消息工具、设置、存储和任务等管理页面的布局、表单交互与可访问性标识。
- 重新生成嵌入式后台资源，并补充新版管理后台与插件市场截图。

### 任务与存储

- 修复插件 `@cron` 虚拟任务的启停、编辑和状态回传，补齐相关回归测试。
- BoltDB 与 Redis 读取整数、布尔值时兼容 `d:` / `b:` 类型前缀，避免旧数据解析异常。

### 插件与文档

- 插件配置删除改为幂等处理，并严格校验本地插件名称与源码元数据一致。
- 重组 API、存储、适配器和插件开发文档，新增可复用的插件编写 skill。

## v1.0.8 - 2026-08-07

### API 与性能

- 管理端 API 统一为 GET / POST，并按 RESTful 资源路径、状态码和 `application/problem+json` 错误格式收敛。
- 青龙、呆呆与 smallcat 面板统一由 `/api/admin/panels` 一次加载，删除旧的分类型面板接口。
- 删除搬运群名称、脚本、规则及昵称标签等无引用旧接口，减少重复请求和维护面。

### 管理后台

- 拆分大型后台页面为独立视图组件和 composable，插件市场编辑器、依赖管理与设置页面改为按需加载。
- 修复插件卡片设置按钮响应慢、Clawbot 图片过期及登录流程状态同步问题。
- 重新生成嵌入式后台资源并补齐资源可达性 smoke test。

### 安全与稳定性

- 管理接口认证、CORS、响应安全头、GET/POST 限制和已删除路由新增回归审计。
- API handler panic 不再向客户端暴露内部错误原文，详细异常仅记录到服务端日志。
- 完成 Go 全量测试、vet、依赖漏洞扫描、npm audit、HTTP 与 Chrome DevTools MCP 回归。

## v1.0.7 - 2026-08-06

### 安全加固

- 公告的 HTML/Markdown 在进入 `v-html` 前统一经过标签、属性和 URL 白名单清洗，阻断脚本、事件属性、危险协议和嵌入对象造成的存储型 XSS。
- HTTP 服务默认不信任转发头；反向代理部署可通过 `SILLYGIRL_TRUSTED_PROXIES` 显式配置可信代理，避免伪造 `X-Forwarded-For` 绕过登录限制。
- 管理员和普通用户登录共用有界的 IP/账号双维度限流，阻断随机用户名喷洒并限制内存记录规模。
- 完成 Go 依赖漏洞扫描、npm production audit、敏感信息扫描和安全回归测试。

### 稳定性与数据一致性

- 修复普通用户并发提交同一插件表单时可能丢失记录的问题，并让用户删除、插件卸载与表单写入共享原子写锁。
- 修复插件配置存储失败仍向前端返回成功的问题。
- 清理启动入口未使用的全局状态、调试注释、历史日志、截图、缓存和临时审计文件。

### 插件仓库

- 对 125 个插件逐个执行实际加载与表单注册审计，NodeJS 15 个、Python 110 个全部通过，未发现重复 UUID、缺失元数据或无效规则。
- 完成 Python 3.12 AST/pyflakes、NodeJS 语法、依赖注释、文件名与版本规则检查。
- 等价折叠旧授权移除流程遗留的恒真表达式，删除未被调用的旧推送函数，并重新生成 125 项插件市场索引。

## v1.0.6 - 2026-08-06

### 插件 SDK

- 插件配置入口统一为 `new plugin.Form({...})`，普通用户表单统一为 `new user.Form({...})`；Python 使用 `plugin.Form({...})` / `user.Form({...})`。
- 同步更新 NodeJS、Python、配置注册器、插件识别、类型声明、测试、开发文档及插件仓库，移除小写 `plugin.form` / `user.form` 导出。
- 统一 SDK 命名：`Form` 仅保留 `get/set`，`Bucket.len()` 改为 `count()`，`Sender.continue()` 改为 `resume()`，`SmallCat.oAuth()` 改为 `oauth()`，并隐藏 Bucket 值转换实现。

### 插件仓库与市场

- 清理插件仓库临时产物和无效缓存，重新生成 `publicFileIndex.json`。
- 批量压缩 Python/NodeJS 插件重复兼容层，删除重复授权、支付、续费类死代码，降低插件体积。
- 修复插件仓库 Python/NodeJS 语法、未定义变量和 Python 3.12 兼容问题。
- 插件市场索引同步更新到 125 个可检测插件，其中 113 个插件已通过新版 `Form` 注册验证。

### 稳定性与构建

- 重新执行 Go 单测、前端正式构建、插件 Python/NodeJS 语法检查和 no-undef 检查。
- 清理旧预览日志、pycache 和临时检查文件，重新生成后台静态资源。

## v1.0.5 - 2026-08-04

### 迁移与卸载

- 新增启动前自动执行的数据字段迁移，独立迁移程序 `storage-migrate` 同步纳入发布包。
- 插件卸载确认改为大弹窗，并支持勾选“同步删除插件配置”。
- 修复旧版插件配置、用户绑定、插件授权和面板桶字段迁移遗漏。

### 插件 SDK

- 容器入口统一改为 `container` 单例：`container.getList()` 可直接调用，`QingLong`、`SmallCat`、`DaiDai` 继续通过 `new container.X({ id })` 创建面板客户端。
- 移除文档、模板和插件示例里的 `new Container()` 用法。
- 老式方括号注释继续兼容普通元数据（如 `// [title: xxx]`、`# [rule: xxx]`），但废弃并忽略 `[param: {...}]` 配置注释；插件配置统一使用顶层 `form`。

### 插件市场编辑器

- 插件市场卡片图标支持打开源码编辑器，提供格式化、保存和删除。
- 插件市场工具栏新增“新增插件”，保存时校验必要注释并强制作为本地非公开插件加入市场。
- 移除左侧“脚本插件”独立页面入口，脚本编辑统一收敛到插件市场。
- 插件市场新增操作提示：点击插件图标编辑源码，点击插件名称查看介绍。

### 普通用户公告

- 基础设置新增普通用户公告开关、格式和公告内容。
- `/user` 页面顶部支持展示管理员配置的多行公告，并支持纯文本、Markdown 和 HTML 渲染。

### 网络代理与镜像

- GitHub 加速、pnpm 镜像、pipx 源支持在基础设置里自定义添加和删除。
- 默认内置常用 GitHub 加速、npm registry 和 PyPI 镜像列表，默认项保留不可删除。

### 清理与构建

- 更新 README、插件开发文档和 skills 插件规范。
- 清理旧前端构建资源并重新生成正式版静态资源。

## v1.0.4 - 2026-08-04

### 插件运行时与配置

- 重构插件配置导出为顶层 `new form({...})` 链式 API，移除旧配置导出入口，插件版本示例维持 `v1` 并按补丁号自增。
- 新增统一 `container` 运行时入口，支持 `QingLong`、`DaiDai`、`SmallCat` 读取容器能力和后续面板操作。
- 插件设置弹窗支持 SmallCat、青龙、呆呆容器编号按后台容器列表动态渲染圆形单选；同步目标切换时只展示对应面板编号。
- 修复带空格的 `new form (...)` 无法被插件配置检测识别的问题，并补充 Node.js/Python 表单注册测试。

### 文档与发布清理

- 更新 README、插件开发文档和技能文档中的 `form`、`container`、`sender`、`utils` 导出关系。
- 更新前端插件配置错误提示，避免继续提示旧版 `SillyGirlPluginConfig` 或 JSON Schema 写法。
- 清理发布前临时产物并重新生成后台静态资源。

## v1.0.3 - 2026-08-04

### 插件开放与用户授权

- 新增插件市场非公开筛选和插件开放入口，普通用户可在 Home 页面选择是否授权插件读取其 SmallCat 账号。
- 新增 Node.js/Python 顶层 `userList()`，向插件提供当前插件范围内的用户、QQ、Telegram 和脱敏后的 SmallCat 授权视图。
- 授权在插件关闭、禁用、未声明 SmallCat 或用户禁用后立即失效；撤销授权同步清理存储记录。
- 插件运行时用户视图改为只读，禁止写入、删除和监听。

### SmallCat 容器

- 按官方 client 实现改用 `/api/accounts` 的实际列表统计账号数量，修复 `/api/auth/validate` 固定 `count=0` 导致的错误显示。
- 新增获取 OpenID 列表按钮和可复制弹窗；列表接口仅返回 OpenID，不向浏览器暴露 API AUTH。
- SmallCat 列表移除 API AUTH 列，编辑时允许留空保留原 AUTH，并修复检测结果未持久化的问题。

### BOT、用户与稳定性

- 完善 Web Bot、钉钉和 QQ 官方频道适配器，补充 QQ Webhook/WebSocket 接入和运行状态管理。
- 用户管理支持新增、删除、禁用账号以及修改昵称、密码、QQ、Telegram 和 SmallCat OpenID 绑定。
- 修复插件子进程启动错误被吞、未启动进程进入进程表以及后台等待竞态问题。
- 修复 Windows PID 复用可能导致进程启动时结束自身的问题。
- 修复 `utils.Contains` 恒真和 `Unique` 遇到非字符串值崩溃的问题。
- 清理旧前端构建资源、无用备份文件、固定外网等待测试和废弃注释代码。

## v1.0.2 - 2026-08-03

### 管理后台

- 重构插件市场卡片：精简展示版本、分类、口令和作者信息，点击插件名称或图标查看详情。
- 已安装插件使用卸载图标，并在卸载前增加二次确认。
- 在插件卡片旁增加齿轮设置入口，配置改为弹窗编辑，统一提供保存和取消操作。
- 重构 BOT 管理页为卡片布局，增加启用状态、连接状态、播放/暂停和齿轮设置操作。
- 增加 Web Bot 右下角悬浮聊天入口，并将 Web、钉钉、QQ 官方频道等适配器纳入 BOT 菜单。
- 更新后台 Logo、响应式布局、表单交互和可访问性细节。

### 定时任务与消息

- 新增定时任务时可选择接收平台并填写接收人。
- 定时任务执行结果按配置发送给指定平台和接收人。
- 修复任务、回复、消息搬运和插件订阅等列表在越界分页下的稳定性问题。

### 插件与依赖

- 插件安装或更新后自动检测 Node.js/Python 缺失依赖，并询问是否自动安装。
- 依赖安装完成后自动重新加载插件配置表单，统一 Node.js 与 Python 插件行为。
- 修复 pnpm 卸载依赖时错误携带 registry 参数的问题，并补充单元测试。
- 文件管理新增脚本插件时，脚本后缀改为可选并自动推断。

### 存储桶与稳定性

- 修复选择存储桶后无法按 key 或 value 查询的问题。
- 增加服务端分页、大小写不敏感查询、稳定排序和非法页码保护。
- 补充定时任务接收人、存储桶分页和依赖命令相关测试。
- 清理无用代码与旧版构建资源，重新生成正式版前端静态资源。
